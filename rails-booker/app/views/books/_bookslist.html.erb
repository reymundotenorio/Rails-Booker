


<table class="table table-striped table-bordered" style="margin-top:20px;">
  <thead>
    <tr>
      <th style="text-align:center; background-color: #80cbc4;"><%= t('book.title') %></th>
      <th style="text-align:center; background-color: #80cbc4;"><%= t('book.Author') %></th>
      <th style="text-align:center; background-color: #80cbc4;"><%= t('book.description') %></th>
      <th colspan="3" style="text-align:center; background-color: #80cbc4;"><%= t('book.options') %></th>
    </tr>
  </thead>


<tbody>
<% @books.each do |book| %>
  <tr>
    <td><%= book.tittle %></td>
    <td><%= book.author %></td>
    <td><%= book.description %></td>
    <td><%= link_to t('book.show'), book, class:"btn btn-primary", style:"color:#FFFFFF;", title:t('book.show')%></td>
    <td><%= link_to t('book.edit'), edit_book_path(book), class:"btn btn-warning", style:"color:#FFFFFF;", title:t('book.edit') %></td>
    <td><%= link_to t('book.delete'), book, class:"btn btn-danger", style:"color:#FFFFFF;", title:t('book.delete'),
    method: :delete, data: { confirm: t('book.sure') } %></td>
  </tr>
<% end %>
  </tbody>

</table>

<br>


<button class="btn btn-primary" type="button" style="float: right;">
  <%= t('book.count') %> <span class="badge"><%= @number_book %></span>
</button>

<!--
#Will Paginate Original
will_paginate @books

will_paginate @books, renderer: BootstrapPagination::Rails

#Will Paginate Bootstrap BluBlu
will_paginate(@books, :renderer => WillPaginate::ActionView::LinkRenderer)
-->


<%= link_to t('book.new'), new_book_path, class: "btn btn-success", title:t('book.new'),
style:"color:#FFFFFF; margin-top:20px;"%>

<%= will_paginate @books, renderer: BootstrapPagination::Rails %>
